function make_shirt(size, text) {
    console.log("The size pf the T.shirt is ".concat(size, " and the message that will be printed on it is ").concat(text));
}
make_shirt("medium", "Coding is fun!");
